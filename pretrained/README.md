# External pretrained artifacts

Binary checkpoints are not redistributed in this source release. Obtain them
from the original STAR/SGG-ToolKit release or from the project's authorized
artifact store, then place compatible files here as referenced by the configs.

The tracked `SF_list_support.json` file is lightweight semantic-support
metadata, not a learned weight file.
